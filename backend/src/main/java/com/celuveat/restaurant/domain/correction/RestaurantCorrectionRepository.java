package com.celuveat.restaurant.domain.correction;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RestaurantCorrectionRepository extends JpaRepository<RestaurantCorrection, Long> {
}
