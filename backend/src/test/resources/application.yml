spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create

server:
  servlet:
    contextPath: /api

cors:
  allowedOrigins: "CORS_ALLOW_ORIGINS"
